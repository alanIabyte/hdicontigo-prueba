query($usuario: String!) {
  notificaciones_obtenerNotificaciones(usuario: $usuario) {
    codigo
    completado
    mensaje
    dato {
      id
      titulo
      contenido
      leido
      tipo
      infoadicional
      fechaalta
    }
  }
}