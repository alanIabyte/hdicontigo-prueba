mutation ($usuario: String!, $publicKeyCredentialId: String!, $token: String!) {
  RegisterKeyCredential(
    usuario: $usuario
    publicKeyCredentialId: $publicKeyCredentialId
    token: $token
  ) {
    codigo
    completado
    mensaje
    dato
  }
}
